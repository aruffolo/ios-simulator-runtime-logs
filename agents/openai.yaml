interface:
  display_name: "iOS Simulator Runtime Logs"
  short_description: "Capture Xcode-like console logs, unified logs, and build-aware simulator sessions"
  default_prompt: "Use $ios-simulator-runtime-logs for iOS Simulator runtime debugging when the user wants the same logs seen in Xcode, needs to build/install before capture, wants unified-log mode for Logger or OSLog, or needs safe handling of multiple app schemes without guessing."
